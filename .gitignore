# Rust
/target/
**/target/
**/*.rs.bk
*.pdb

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# dev orchestration usage log
dev/.cloud-usage.tsv
**/.cloud-usage.tsv

# staging scratch for guest deployment
.tmp/

# oxrdp's own Windows guest (disk image, shared folder, generated password)
.vm/

# cargo-fuzz artifacts (generated by `cargo fuzz` runs)
fuzz/corpus/
fuzz/Cargo.lock
